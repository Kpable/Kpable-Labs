using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Kpable.Utilities;

public class ReflectiveSurface : MonoBehaviour {
    [Tooltip("Whether reflection is one of the 8 directions or if its dynamic")]
    public bool fixedReflection;
    //public Direction reflectDirection;
	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
